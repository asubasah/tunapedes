# Daily Log: 2026-04-12

## Aktivitas Resolusi FitMotor CRM
- **Sesi Malam:** Mengeksekusi perbaikan "Enterprise" untuk aliran data AI.
- **Bug n8n v1:** Menemukan bahwa Model Node tidak bisa berdiri sendiri di tengah Flow tanpa disematkan ke `Basic LLM Chain` atau `Agent`. Node yang telanjang akan berakibat kosong / NULL output yang mematikan trigger Intent.
- **Auto-Memory:** Menambahkan node auto-save di file `02` dan `03` menggunakan statement SQL `INSERT IGNORE... ON DUPLICATE KEY UPDATE` agar pelanggan yang sudah pernah sukses booking otomatis masuk permanent storage (`pelanggan`), mencegah amnesia di sesi selanjutnya.
- **Google Maps Hijack:** Menyuntikkan pencegat Share Location di `01_main_router.json`. Pesan kosong dengan metadata koordinat dari Waha/GoWA kini dikonversi menjadi format string tertulis, sehingga AI tahu koordinatnya. Dan ini dipanggil secara absolut di Google Maps HTTP node.
- **Troubleshooting Env Node n8n:** Menjumpai `access to env vars denied` di Node HTTP Gmaps karena limitasi Security N8N bawaan untuk pembacaan `$env`. Di-bypass menggunakan opsi `Continue on Fail` dan pemisahan custom default value *try catch* di JS Node.
- **Fitur Cek Booking Kustom (File 05):** Atas permintaan klien yang super cerdas, kita berkolaborasi membangun sistem satpam data. `Basic LLM Chain` di File 01 dirombak instruksinya untuk melempar Intent `cek_booking` ke webhook `Route: Cek Booking Flow`. File 05 mencegah nomor Asing melihat Nopol, dan membunyikan alarm peringatan di WA pemilik aslinya. Status Servis `pending`, `proses`, dan `selesai` kini ditracking real-time layaknya Resi J&T.

**Rencana ke depan:**
Semua pondasi permesinan (Backend API/N8N) telah selesai absolut. Tahap logis selanjutnya adalah membangun Dashboard Admin Web Frontend untuk bengkel/kru mekanik bisa menekan tombol "Proses" atau "Selesai", yang akan langsung berimbas di SQL-Database dan otomatis termonitoring oleh jawaban Agen Bot.
